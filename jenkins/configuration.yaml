{
  "credentials": [
    {
      "scope": "GLOBAL",
      "id": "Jenkins-testing",
      "username": "admin",
      "password": "password",
      "description": "Jenkins admin credentials"
    }
  ],
  "jobs": [
    {
      "name": "example-job",
      "description": "This is an example job that uses a Jenkinsfile",
      "jenkinsfile": "pipeline { agent any; stages { stage('Build') { steps { sh './gradlew clean build --stacktrace -i' } } } }"
    }
  ]
}
